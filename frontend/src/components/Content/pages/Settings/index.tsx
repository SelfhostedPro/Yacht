import React from "react";
import ContentWrapper from "../../ContentWrapper";

const Settings = () => {
  return <ContentWrapper header={"Settings"}>WORKING</ContentWrapper>;
};

export default Settings;
